{
  "name": "portisoccupied",
  "version": "0.0.2",
  "description": "Check if a port is occupied",
  "main": "lib/index.js",
  "scripts": {
    "tsc": "tsc",
    "test": "jest --config .jest.js --runInBand",
    "version": "standard-version -- --release-as"
  },
  "repository": {
    "type": "git",
    "url": "git+git@github.com:Nelayah/portIsOccupied.git"
  },
  "keywords": [
    "node",
    "port"
  ],
  "author": "hayalen",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "latest",
    "@types/node": "latest",
    "cz-conventional-changelog": "^2.1.0",
    "jest": "latest",
    "ramda": "latest",
    "standard-version": "^4.4.0",
    "ts-jest": "latest",
    "tslint": "latest",
    "typescript": "latest"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
